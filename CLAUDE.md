# このリポジトリで作業する Claude へ

同じリポジトリを複数の Claude セッションが同時に触ることがあります。実際に事故が起きた経験から、次の3点を守ってください。

## 1. 着手前に main と PR を見る

```bash
git fetch && git log --oneline -10 origin/main
gh pr list
```

自分が直そうとしているものが、すでに別のセッションの手で main に入っていたり、PR になっていたりします。これを飛ばしたために、同じコミットが2つの PR（#13 と #14）で二重に出ました。

## 2. 他のセッションのコミットを取り込む前に一声かける

別のワークツリーやブランチにあるコミットを cherry-pick したり、他のセッションの未コミットの変更を自分の PR に含めたりする前に、その相手に連絡してください（`ListAgents` で一覧、`SendMessage` で連絡）。相手が同じものを PR にしようとしているかもしれません。

## 3. main ref を動かさない

**共有チェックアウト `/Users/casual/git/abduction-learning` と、そこから生えた各ワークツリーのどちらでも、`main` ブランチの ref を動かすコマンドを使わないでください。**

```bash
git switch --detach origin/main   # ○ これを使う
git checkout -B main origin/main  # ✗ 事故のもと
```

`-B` はローカルの `main` を force で動かします。共有チェックアウトは HEAD が symbolic ref で `main` を指しているため、HEAD だけが進み、index と実ファイルは古いコミットのまま取り残されます。その状態は `git status` では「マージ済みの変更を打ち消すステージ済みの差分」に見え、うっかりコミットすると本番に出したばかりの修正が巻き戻ります。実際に2回発生しました（#12 の直後と、#15/#16 の直後）。

ワークツリーで `main` を触る必要はありません。作業は必ず自分のブランチで行い、`origin/main` は detached で参照してください。

## 共有チェックアウトの扱い

`/Users/casual/git/abduction-learning` は全セッションが見ている場所です。ここに未コミットの変更やステージ済みの差分を残さないでください。見覚えのない差分を見つけたら、消す前に:

- `git log --oneline -5 origin/main` と見比べて、マージ済みの変更を打ち消す内容になっていないか確かめる
- 他のセッションに心当たりを聞く（`ListAgents` → `SendMessage`）
- 捨てても失うものが無いこと（`git diff --cached --name-only <その差分と一致するコミット>` が空、未追跡ファイルが無いこと）を確認してから戻す
